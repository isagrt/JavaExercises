public class Maths {
    public static void main (String[] agrs){

        double raiz = Math.sqrt(25);
        int maior = Math.max(10,80);
        int menor = Math.min(53,98);
        double potenciacao = Math.pow(2, 3);
        double absoluto = Math.abs(56);
        double arredondado = Math.round(25.89);
        int numeroRandomico = (int) (Math.random() * 100);
 
        System.out.println("Numero: "+numeroRandomico);
        System.out.println("Numero: "+maior);
    }
}